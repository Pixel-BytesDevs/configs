server:
  port: 8083
  address: 0.0.0.0

spring:
  security:
        oauth2:
          resourceserver:
            jwt:
              issuer-uri: http://localhost:9003
  neo4j:
    uri: ${NEO4J_URI}
    authentication:
      username: ${NEO4J_USER}
      password: ${NEO4J_PASS}